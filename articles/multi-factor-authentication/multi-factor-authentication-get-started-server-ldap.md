---
title: Authentification LDAP et serveur Azure MFA | Microsoft Docs
description: "Il s'agit de la page d'authentification multifacteur Azure qui facilite le déploiement de l’authentification LDAP et du serveur Azure Multi-Factor Authentication."
services: multi-factor-authentication
documentationcenter: 
author: kgremban
manager: femila
editor: yossib
ms.assetid: e1a68568-53d1-4365-9e41-50925ad00869
ms.service: multi-factor-authentication
ms.workload: identity
ms.tgt_pltfrm: na
ms.devlang: na
ms.topic: article
ms.date: 05/03/2017
ms.author: kgremban
ms.openlocfilehash: 8f4d5f9e84ad7bb4fff501370036e7f0da589bf3
ms.sourcegitcommit: f537befafb079256fba0529ee554c034d73f36b0
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 07/11/2017
---
# <a name="ldap-authentication-and-azure-multi-factor-authentication-server"></a><span data-ttu-id="ace17-103">Authentification LDAP et serveur Azure Multi-Factor Authentication</span><span class="sxs-lookup"><span data-stu-id="ace17-103">LDAP authentication and Azure Multi-Factor Authentication Server</span></span>
<span data-ttu-id="ace17-104">Par défaut, le serveur Azure Multi-Factor Authentication est configuré pour importer ou synchroniser des utilisateurs à partir d'Active Directory.</span><span class="sxs-lookup"><span data-stu-id="ace17-104">By default, the Azure Multi-Factor Authentication Server is configured to import or synchronize users from Active Directory.</span></span> <span data-ttu-id="ace17-105">Toutefois, il peut être configuré pour être lié à d’autres répertoires LDAP, comme un répertoire ADAM ou un contrôleur de domaine Active Directory spécifique.</span><span class="sxs-lookup"><span data-stu-id="ace17-105">However, it can be configured to bind to different LDAP directories, such as an ADAM directory, or specific Active Directory domain controller.</span></span> <span data-ttu-id="ace17-106">Lorsqu’il est connecté à un annuaire par LDAP, le serveur Azure Multi-Factor Authentication peut servir de proxy LDAP pour effectuer les authentifications.</span><span class="sxs-lookup"><span data-stu-id="ace17-106">When connected to a directory via LDAP, the Azure Multi-Factor Authentication Server can act as an LDAP proxy to perform authentications.</span></span> <span data-ttu-id="ace17-107">Il permet également d’utiliser la liaison LDAP comme cible RADIUS, pour pré-authentifier les utilisateurs avec l’authentification IIS ou pour effectuer l’authentification principale dans le portail de l’utilisateur Azure MFA.</span><span class="sxs-lookup"><span data-stu-id="ace17-107">It also allows for the use of LDAP bind as a RADIUS target, for pre-authentication of users with IIS Authentication, or for primary authentication in the Azure MFA user portal.</span></span>

<span data-ttu-id="ace17-108">Pour utiliser Azure Multi-Factor Authentication comme proxy LDAP, insérez le serveur Azure Multi-Factor Authentication entre le client LDAP (par exemple, un équipement VPN ou une application) et le serveur d’annuaire LDAP.</span><span class="sxs-lookup"><span data-stu-id="ace17-108">To use Azure Multi-Factor Authentication as an LDAP proxy, insert the Azure Multi-Factor Authentication Server between the LDAP client (for example, VPN appliance, application) and the LDAP directory server.</span></span> <span data-ttu-id="ace17-109">Le serveur Azure Multi-Factor Authentication doit être configuré pour communiquer avec les serveurs des clients et l’annuaire LDAP.</span><span class="sxs-lookup"><span data-stu-id="ace17-109">The Azure Multi-Factor Authentication Server must be configured to communicate with both the client servers and the LDAP directory.</span></span> <span data-ttu-id="ace17-110">Dans cette configuration, le serveur Azure Multi-Factor Authentication accepte les requêtes LDAP provenant des serveurs clients et des applications, puis les transmet au serveur de répertoire LDAP cible pour valider les informations d'identification principales.</span><span class="sxs-lookup"><span data-stu-id="ace17-110">In this configuration, the Azure Multi-Factor Authentication Server accepts LDAP requests from client servers and applications and forwards them to the target LDAP directory server to validate the primary credentials.</span></span> <span data-ttu-id="ace17-111">Si l’annuaire LDAP valide les informations d’identification principales, Azure Multi-Factor Authentication effectue une seconde vérification d’identité et renvoie une réponse au client LDAP.</span><span class="sxs-lookup"><span data-stu-id="ace17-111">If the LDAP directory validates the primary credentials, Azure Multi-Factor Authentication performs a second identity verification and sends a response back to the LDAP client.</span></span> <span data-ttu-id="ace17-112">L’authentification n’est complète que si l’authentification du serveur LDAP et la vérification en deux étapes aboutissent.</span><span class="sxs-lookup"><span data-stu-id="ace17-112">The entire authentication succeeds only if both the LDAP server authentication and the second-step verification succeed.</span></span>

## <a name="configure-ldap-authentication"></a><span data-ttu-id="ace17-113">Configurer l’authentification LDAP</span><span class="sxs-lookup"><span data-stu-id="ace17-113">Configure LDAP authentication</span></span>
<span data-ttu-id="ace17-114">Pour configurer l'authentification LDAP, installez le serveur Azure Multi-Factor Authentication sur un serveur Windows.</span><span class="sxs-lookup"><span data-stu-id="ace17-114">To configure LDAP authentication, install the Azure Multi-Factor Authentication Server on a Windows server.</span></span> <span data-ttu-id="ace17-115">Procédez comme suit :</span><span class="sxs-lookup"><span data-stu-id="ace17-115">Use the following procedure:</span></span>

### <a name="add-an-ldap-client"></a><span data-ttu-id="ace17-116">Ajouter un client LDAP</span><span class="sxs-lookup"><span data-stu-id="ace17-116">Add an LDAP client</span></span>

1. <span data-ttu-id="ace17-117">Dans le serveur Azure Multi-Factor Authentication, cliquez sur l’icône d’authentification LDAP dans le menu de gauche.</span><span class="sxs-lookup"><span data-stu-id="ace17-117">In the Azure Multi-Factor Authentication Server, select the LDAP Authentication icon in the left menu.</span></span>
2. <span data-ttu-id="ace17-118">Cochez la case **Activer l’authentification LDAP**.</span><span class="sxs-lookup"><span data-stu-id="ace17-118">Check the **Enable LDAP Authentication** checkbox.</span></span>

   ![Authentification LDAP](./media/multi-factor-authentication-get-started-server-ldap/ldap2.png)

3. <span data-ttu-id="ace17-120">Dans l’onglet Clients, modifiez le port TCP et le port SSL si le service LDAP Azure Multi-Factor Authentication doit se connecter à des ports non standard pour écouter les requêtes LDAP.</span><span class="sxs-lookup"><span data-stu-id="ace17-120">On the Clients tab, change the TCP port and SSL port if the Azure Multi-Factor Authentication LDAP service should bind to non-standard ports to listen for LDAP requests.</span></span>
4. <span data-ttu-id="ace17-121">Si vous envisagez d'utiliser LDAPS du client au serveur Azure Multi-Factor Authentication (MFA), un certificat SSL doit être installé sur le même serveur comme serveur MFA.</span><span class="sxs-lookup"><span data-stu-id="ace17-121">If you plan to use LDAPS from the client to the Azure Multi-Factor Authentication Server, an SSL certificate must be installed on the same server as MFA Server.</span></span> <span data-ttu-id="ace17-122">Cliquez sur **Parcourir** en regard de la zone Certificat SSL et sélectionnez un certificat à utiliser pour la connexion sécurisée.</span><span class="sxs-lookup"><span data-stu-id="ace17-122">Click **Browse** next to the SSL certificate box, and select a certificate to use for the secure connection.</span></span>
5. <span data-ttu-id="ace17-123">Cliquez sur **Add**.</span><span class="sxs-lookup"><span data-stu-id="ace17-123">Click **Add**.</span></span>
6. <span data-ttu-id="ace17-124">Dans la boîte de dialogue Ajouter un client LDAP, entrez l’adresse IP de l’appliance, du serveur ou de l’application qui s’authentifie auprès du serveur, et un nom d’application (facultatif).</span><span class="sxs-lookup"><span data-stu-id="ace17-124">In the Add LDAP Client dialog box, enter the IP address of the appliance, server, or application that authenticates to the Server and an Application name (optional).</span></span> <span data-ttu-id="ace17-125">Le nom de l'application apparaît dans les rapports du serveur Azure Multi-Factor Authentication et peut être affiché dans les messages d'authentification SMS ou d’application mobile.</span><span class="sxs-lookup"><span data-stu-id="ace17-125">The Application name appears in Azure Multi-Factor Authentication reports and may be displayed within SMS or Mobile App authentication messages.</span></span>
7. <span data-ttu-id="ace17-126">Cochez la case de **correspondance d’utilisateur Authentification multifacteur Azure requise** si tous les utilisateurs ont été ou seront importés sur le serveur et soumis à la vérification en deux étapes.</span><span class="sxs-lookup"><span data-stu-id="ace17-126">Check the **Require Azure Multi-Factor Authentication user match** box if all users have been or will be imported into the Server and subject to two-step verification.</span></span> <span data-ttu-id="ace17-127">Si un grand nombre d’utilisateurs n’ont pas encore été importés sur le serveur et/ou ne sont pas soumis à la vérification en deux étapes, laissez la case désactivée.</span><span class="sxs-lookup"><span data-stu-id="ace17-127">If a significant number of users have not yet been imported into the Server and/or are exempt from two-step verification, leave the box unchecked.</span></span> <span data-ttu-id="ace17-128">Voir le fichier d’aide du serveur MFA pour plus d’informations sur cette fonctionnalité.</span><span class="sxs-lookup"><span data-stu-id="ace17-128">See the MFA Server help file for additional information on this feature.</span></span>

<span data-ttu-id="ace17-129">Répétez ces étapes pour ajouter d’autres clients LDAP.</span><span class="sxs-lookup"><span data-stu-id="ace17-129">Repeat these steps to add additional LDAP clients.</span></span>

### <a name="configure-the-ldap-directory-connection"></a><span data-ttu-id="ace17-130">Configurer la connexion à l’annuaire LDAP</span><span class="sxs-lookup"><span data-stu-id="ace17-130">Configure the LDAP directory connection</span></span>

<span data-ttu-id="ace17-131">Lorsque l'authentification multifacteur Azure est configurée pour recevoir des authentifications LDAP, il doit transférer via un proxy ces authentifications au répertoire LDAP.</span><span class="sxs-lookup"><span data-stu-id="ace17-131">When the Azure Multi-Factor Authentication is configured to receive LDAP authentications, it must proxy those authentications to the LDAP directory.</span></span> <span data-ttu-id="ace17-132">Par conséquent, l'onglet Cible affiche une seule option grisée pour utiliser une cible LDAP.</span><span class="sxs-lookup"><span data-stu-id="ace17-132">Therefore, the Target tab only displays a single, grayed out option to use an LDAP target.</span></span>

1. <span data-ttu-id="ace17-133">Pour configurer la connexion à l’annuaire LDAP, cliquez sur l’icône **Intégration d’annuaire**.</span><span class="sxs-lookup"><span data-stu-id="ace17-133">To configure the LDAP directory connection, click the **Directory Integration** icon.</span></span>
2. <span data-ttu-id="ace17-134">Sous l’onglet Paramètres, sélectionnez le bouton radio **Utiliser une configuration LDAP spécifique**.</span><span class="sxs-lookup"><span data-stu-id="ace17-134">On the Settings tab, select the **Use specific LDAP configuration** radio button.</span></span>
3. <span data-ttu-id="ace17-135">Sélectionnez **Modifier…**.</span><span class="sxs-lookup"><span data-stu-id="ace17-135">Select **Edit…**</span></span>
4. <span data-ttu-id="ace17-136">Dans la boîte de dialogue Modifier la configuration LDAP, remplissez les champs avec les informations requises pour se connecter au répertoire LDAP.</span><span class="sxs-lookup"><span data-stu-id="ace17-136">In the Edit LDAP Configuration dialog box, populate the fields with the information required to connect to the LDAP directory.</span></span> <span data-ttu-id="ace17-137">Le fichier d’aide du serveur Azure Multi-Factor Authentication fournit une description des champs.</span><span class="sxs-lookup"><span data-stu-id="ace17-137">Descriptions of the fields are included in the Azure Multi-Factor Authentication Server help file.</span></span>

    ![Intégration de répertoires](./media/multi-factor-authentication-get-started-server-ldap/ldap.png)

5. <span data-ttu-id="ace17-139">Testez la connexion LDAP en cliquant sur le bouton **Test**.</span><span class="sxs-lookup"><span data-stu-id="ace17-139">Test the LDAP connection by clicking the **Test** button.</span></span>
6. <span data-ttu-id="ace17-140">Si le test de connexion LDAP a réussi, cliquez sur le bouton **OK**.</span><span class="sxs-lookup"><span data-stu-id="ace17-140">If the LDAP connection test was successful, click the **OK** button.</span></span>
7. <span data-ttu-id="ace17-141">Cliquez sur l’onglet **Filtres**.</span><span class="sxs-lookup"><span data-stu-id="ace17-141">Click the **Filters** tab.</span></span> <span data-ttu-id="ace17-142">Le serveur est préconfiguré pour charger des conteneurs, des groupes de sécurité et des utilisateurs à partir d’Active Directory.</span><span class="sxs-lookup"><span data-stu-id="ace17-142">The Server is pre-configured to load containers, security groups, and users from Active Directory.</span></span> <span data-ttu-id="ace17-143">En cas de liaison à un autre annuaire LDAP, vous devrez probablement modifier les filtres affichés.</span><span class="sxs-lookup"><span data-stu-id="ace17-143">If binding to a different LDAP directory, you probably need to edit the filters displayed.</span></span> <span data-ttu-id="ace17-144">Pour plus d’informations sur les filtres, cliquez sur le lien **Aide**.</span><span class="sxs-lookup"><span data-stu-id="ace17-144">Click the **Help** link for more information on filters.</span></span>
8. <span data-ttu-id="ace17-145">Cliquez sur onglet **Attributes** .</span><span class="sxs-lookup"><span data-stu-id="ace17-145">Click the **Attributes** tab.</span></span> <span data-ttu-id="ace17-146">Le serveur est préconfiguré pour mapper les attributs à partir d’Active Directory.</span><span class="sxs-lookup"><span data-stu-id="ace17-146">The Server is pre-configured to map attributes from Active Directory.</span></span>
9. <span data-ttu-id="ace17-147">En cas de liaison à un autre annuaire LDAP ou de modification des mappages d’attributs préconfigurés, cliquez sur **Modifier…**.</span><span class="sxs-lookup"><span data-stu-id="ace17-147">If you're binding to a different LDAP directory or to change the pre-configured attribute mappings, click **Edit…**</span></span>
10. <span data-ttu-id="ace17-148">Dans la boîte de dialogue Modifier les attributs, modifiez les mappages d'attributs LDAP de votre répertoire.</span><span class="sxs-lookup"><span data-stu-id="ace17-148">In the Edit Attributes dialog box, modify the LDAP attribute mappings for your directory.</span></span> <span data-ttu-id="ace17-149">Les noms d’attribut peuvent être saisis ou sélectionnés à l’aide du bouton **…**</span><span class="sxs-lookup"><span data-stu-id="ace17-149">Attribute names can be typed in or selected by clicking the **…**</span></span> <span data-ttu-id="ace17-150">en regard de chaque champ.</span><span class="sxs-lookup"><span data-stu-id="ace17-150">button next to each field.</span></span> <span data-ttu-id="ace17-151">Pour plus d’informations sur les attributs, cliquez sur le lien **Aide**.</span><span class="sxs-lookup"><span data-stu-id="ace17-151">Click the **Help** link for more information on attributes.</span></span>
11. <span data-ttu-id="ace17-152">Cliquez sur le bouton **OK**.</span><span class="sxs-lookup"><span data-stu-id="ace17-152">Click the **OK** button.</span></span>
12. <span data-ttu-id="ace17-153">Cliquez sur l’icône **Paramètres de la société** et sélectionnez l’onglet **Résolution du nom d’utilisateur**.</span><span class="sxs-lookup"><span data-stu-id="ace17-153">Click the **Company Settings** icon and select the **Username Resolution** tab.</span></span>
13. <span data-ttu-id="ace17-154">Si vous vous connectez à Active Directory à partir d’un serveur joint à un domaine, laissez le bouton radio **Utiliser des identificateurs de sécurité Windows (SID) pour les noms d’utilisateur correspondants** sélectionné.</span><span class="sxs-lookup"><span data-stu-id="ace17-154">If you're connecting to Active Directory from a domain-joined server, leave the **Use Windows security identifiers (SIDs) for matching usernames** radio button selected.</span></span> <span data-ttu-id="ace17-155">Sinon, sélectionnez le bouton radio **Utiliser un attribut d’identificateur unique LDAP pour les noms d’utilisateur correspondants**.</span><span class="sxs-lookup"><span data-stu-id="ace17-155">Otherwise, select the **Use LDAP unique identifier attribute for matching usernames** radio button.</span></span> 

<span data-ttu-id="ace17-156">Lorsque le bouton radio **Utiliser un attribut d’identificateur unique LDAP pour les noms d’utilisateur correspondants** est sélectionné, le serveur Azure Multi-Factor Authentication tente de résoudre chaque nom d’utilisateur en un identificateur unique dans l’annuaire LDAP.</span><span class="sxs-lookup"><span data-stu-id="ace17-156">When the **Use LDAP unique identifier attribute for matching usernames** radio button is selected, the Azure Multi-Factor Authentication Server attempts to resolve each username to a unique identifier in the LDAP directory.</span></span> <span data-ttu-id="ace17-157">Une recherche LDAP est effectuée sur les attributs Nom d’utilisateur définis dans l’onglet Intégration d’annuaire -> Attributs.</span><span class="sxs-lookup"><span data-stu-id="ace17-157">An LDAP search is performed on the Username attributes defined in the Directory Integration -> Attributes tab.</span></span> <span data-ttu-id="ace17-158">Lorsqu’un utilisateur s’authentifie, le nom d’utilisateur est résolu avec l’identificateur unique de l’annuaire LDAP.</span><span class="sxs-lookup"><span data-stu-id="ace17-158">When a user authenticates, the username is resolved to the unique identifier in the LDAP directory.</span></span> <span data-ttu-id="ace17-159">L’identificateur unique est utilisé pour faire correspondre l’utilisateur dans le fichier de données Azure Multi-Factor Authentication.</span><span class="sxs-lookup"><span data-stu-id="ace17-159">The unique identifier is used for matching the user in the Azure Multi-Factor Authentication data file.</span></span> <span data-ttu-id="ace17-160">Cela permet des comparaisons sans prise en compte de la casse et autorise les noms d’utilisateur longs et courts.</span><span class="sxs-lookup"><span data-stu-id="ace17-160">This allows for case-insensitive comparisons, and long and short username formats.</span></span>

<span data-ttu-id="ace17-161">Une fois ces opérations terminées, le serveur MFA écoute sur les ports configurés, les demandes d’accès LDAP provenant des clients configurés, et agit comme un proxy pour ces demandes à l’annuaire LDAP à des fins d’authentification.</span><span class="sxs-lookup"><span data-stu-id="ace17-161">After you complete these steps, the MFA Server listens on the configured ports for LDAP access requests from the configured clients, and acts as a proxy for those requests to the LDAP directory for authentication.</span></span>

## <a name="configure-ldap-client"></a><span data-ttu-id="ace17-162">Configurer un client LDAP</span><span class="sxs-lookup"><span data-stu-id="ace17-162">Configure LDAP client</span></span>
<span data-ttu-id="ace17-163">Pour configurer le client LDAP, suivez ces indications :</span><span class="sxs-lookup"><span data-stu-id="ace17-163">To configure the LDAP client, use the guidelines:</span></span>

* <span data-ttu-id="ace17-164">Configurez votre appliance, votre serveur ou votre application pour vous authentifier par LDAP auprès du serveur Azure Multi-Factor Authentication, comme s’il s’agissait de votre annuaire LDAP.</span><span class="sxs-lookup"><span data-stu-id="ace17-164">Configure your appliance, server, or application to authenticate via LDAP to the Azure Multi-Factor Authentication Server as though it were your LDAP directory.</span></span> <span data-ttu-id="ace17-165">Utilisez les mêmes paramètres que pour vous connecter directement à votre annuaire LDAP, sauf pour le nom du serveur ou l’adresse IP qui sera celui ou celle du serveur Azure Multi-Factor Authentication.</span><span class="sxs-lookup"><span data-stu-id="ace17-165">Use the same settings that you would normally use to connect directly to your LDAP directory, except for the server name or IP address, which will be that of the Azure Multi-Factor Authentication Server.</span></span>
* <span data-ttu-id="ace17-166">Réglez le délai d’expiration LDAP sur 30 à 60 secondes pour pouvoir valider les informations d’identification de l’utilisateur dans l’annuaire LDAP, effectuer la vérification en deux étapes, recevoir leur réponse et répondre à la demande d’accès LDAP.</span><span class="sxs-lookup"><span data-stu-id="ace17-166">Configure the LDAP timeout to 30-60 seconds so that there is time to validate the user’s credentials with the LDAP directory, perform the second-step verification, receive their response, and respond to the LDAP access request.</span></span>
* <span data-ttu-id="ace17-167">Si vous utilisez LDAPS, l’appliance ou le serveur qui effectue les requêtes LDAP doit approuver le certificat SSL installé sur le serveur Azure Multi-Factor Authentication.</span><span class="sxs-lookup"><span data-stu-id="ace17-167">If using LDAPS, the appliance or server making the LDAP queries must trust the SSL certificate installed on the Azure Multi-Factor Authentication Server.</span></span>

